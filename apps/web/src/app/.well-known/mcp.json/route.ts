// Alias of /.well-known/mcp/server-card.json for crawlers that probe the
// shorter, conventional path. Both must stay in lockstep — re-export only.
export { GET } from "../mcp/server-card.json/route";
