import { HowItWorks } from "@/components/marketing/how-it-works";

/**
 * This page demonstrates the HowItWorks component standalone.
 * The component is also integrated into the main landing page at (landing)/page.tsx
 * between the hero section and the features section.
 */
export default function MarketingPage() {
  return <HowItWorks />;
}
