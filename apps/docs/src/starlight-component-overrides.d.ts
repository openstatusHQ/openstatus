// Copied Starlight component overrides (Header.astro) import the per-component
// `virtual:starlight/components/*` modules, whose types live in Starlight's
// internal declarations rather than its public `virtual.d.ts`. Pull them into
// scope so `astro check` and the editor can resolve the imports.
/// <reference path="../node_modules/@astrojs/starlight/virtual-internal.d.ts" />
