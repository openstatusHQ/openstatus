TOKEN "endpoint__dns_get_14d__v0_endpoint_read_7283" READ

NODE endpoint
SQL >

%
    SELECT *
    FROM dns_response__v0
    WHERE
    monitorId = {{ String(monitorId, '7417', required=True) }}
    AND id = {{ String(id, '', required=True) }}
    ORDER BY timestamp DESC

TYPE endpoint


