TAGS "icmp"

NODE endpoint
SQL >

    %
        SELECT *
        FROM mv__icmp_full_14d__v0
        WHERE
        monitorId = {{ String(monitorId, '4433', required=True) }}
        AND id = {{ String(id, '', required=True) }}
        ORDER BY time DESC


TYPE ENDPOINT
