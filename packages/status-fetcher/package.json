{
  "name": "@openstatus/status-fetcher",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fetchers": "./src/fetchers/index.ts",
    "./data": "./src/data/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "check": "deno check --sloppy-imports .",
    "test": "deno test --parallel -A --no-check --sloppy-imports",
    "test:watch": "deno test --parallel -A --no-check --sloppy-imports --watch",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "catalog:",
    "node-html-parser": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@std/expect": "jsr:^1.0.19",
    "@std/testing": "jsr:^1.0.19"
  }
}
